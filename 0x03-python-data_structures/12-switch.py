#!/usr/bin/python3

# Initialize two variables
a = 89
b = 10

# Swap the values of the variables using tuple unpacking
a, b = b, a

# Print the swapped values
print("After swapping: a={:d} - b={:d}".format(a, b))
